# Portfolio (React)

This project was migrated from a static `index.html` portfolio to a React app using Vite.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

Legacy files are in `legacy/`.
